# ================================================================
# Vega FastAPI 백엔드 Dockerfile
# 위치: backend/Dockerfile
# 배포: Railway (GitHub push 자동 배포)
# ================================================================

FROM python:3.11-slim

# 작업 디렉터리 설정
WORKDIR /app

# 시스템 의존성 설치 (bcrypt 컴파일에 필요)
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# Python 의존성 먼저 복사 (레이어 캐시 최적화)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 애플리케이션 코드 복사 (.env는 Railway 환경변수로 주입, 복사 안 함)
COPY app/ ./app/

# 포트 노출 (Railway가 PORT 환경변수를 주입함)
EXPOSE 8000

# 서버 실행
# Railway는 PORT 환경변수를 자동으로 설정함
CMD ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
